// People tab → taste-twins (spec §3 screen 4). The old FriendsScreen (trust
// directory) stays in features/activity as reference until removed.
import { PeopleScreen } from '@/features/taste';

export default PeopleScreen;
