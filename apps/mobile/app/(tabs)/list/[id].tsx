// List detail (v3.1) — shows the list's vouches grouped by category. The
// legacy destination-item list-detail screen is kept in the feature folder
// as a fallback but is no longer the route.
import { ListDetailScreen } from '@/features/lists';

export default ListDetailScreen;
