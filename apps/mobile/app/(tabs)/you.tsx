// You tab → public taste identity + account home (taste pivot, spec §3b).
// The old ProfileScreen (trust-era travel identity) stays in features/profile
// as reference until removed.
import { YouScreen } from '@/features/taste';

export default YouScreen;
